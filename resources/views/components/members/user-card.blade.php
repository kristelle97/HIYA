<div class="user-card card">
    <img class="mx-auto d-block mb-3 user-image shadow" src="{{$user->picture_url}}"/>

    <div class="card-body">
        <h5 class="text-center">{{$user->full_name}}</h5>
        <p class="text-center">{{$user->country_of_residence}}</p>
    </div>
</div>
